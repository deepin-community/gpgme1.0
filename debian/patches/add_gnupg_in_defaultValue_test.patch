Index: gpgme1.0/lang/qt/tests/t-config.cpp
===================================================================
--- gpgme1.0.orig/lang/qt/tests/t-config.cpp
+++ gpgme1.0/lang/qt/tests/t-config.cpp
@@ -62,7 +62,8 @@ private Q_SLOTS:
         auto entry = conf->entry(QStringLiteral("gpg"), QStringLiteral("compliance"));
         QVERIFY(entry);
         const auto defaultValue = entry->defaultValue().toString();
-        QCOMPARE(defaultValue, QStringLiteral("openpgp"));
+        QVERIFY(defaultValue == QStringLiteral("openpgp") || defaultValue == QStringLiteral("gnupg"));
+
 
         entry->setStringValue("de-vs");
         conf->sync(true);
